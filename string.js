// write javascript code  that takes a string as input and uses 
//string  methods to count the number of characters in the string, excluding spaces then display
//the characters count.
// for example if the input is "hello, world!"


//METHOD1

let text = "hello no";
console.log(text);
let textbeforelength = text.length;
console.log(textbeforelength);
let replacetext = text.replaceall("", "");
console.log(replacetext);
let replacetextlength = replacetext.length;
console.log(replacetextlength);

// using the replace method
